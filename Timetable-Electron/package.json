{
  "name": "timetable",
  "version": "1.8.0",
  "description": "timetable app",
  "main": "main.js",
  "scripts": {
    "start": "electron main.js",
    "package": "",
    "build": "",
    "package-mac": "electron-packager ./ --overwrite --platform=darwin --arch=x64 --icon=assets/icons/icon.icns --prune=true --out=release-builds",
    "package-win": "electron-packager ./ electron-tutorial-app --overwrite --asar=true --platform=win32 --arch=x64 --icon=assets/icons/icon.ico --prune=true --out=release-builds --version-string.CompanyName=CE --version-string.FileDescription=CE --version-string.ProductName=\"Timetable\"",
    "package-linux": "electron-packager ./ electron-tutorial-app --overwrite --asar=true --platform=linux --arch=x64 --icon=assets/icons/icon.png --prune=true --out=release-builds"
  },
  "icon": "assets/icons/icon.ico",
  "displayName": "Timetable",
  "bin": "TT01",
  "author": "Anthonym",
  "license": "ISC",
  "devDependencies": {
    "asar": "^1.0.0",
    "electron": "^4.2.12",
    "electron-packager": "^13.1.1"
  },
  "dependencies": {
    "electron-window-state": "^5.0.3",
    "fs-extra": "^8.1.0"
  }
}
